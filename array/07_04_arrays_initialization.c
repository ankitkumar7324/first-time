#include<stdio.h>
int main()
{
    // int a[]={34,56,23};
    // printf("the value of a[o] is %d\n",a[0]);
    // printf("the value of a[1] is %d\n",a[1]);
    // printf("the value of a[2] is %d\n",a[2]);
    float a[]={34.3,56.43,23.34};
    printf("the value of a[o] is %f\n",a[0]);
    printf("the value of a[1] is %f\n",a[1]);
    printf("the value of a[2] is %f\n",a[2]);
    return 0;
}